__pycache__/
.pytest_cache/
*.egg-info/
*.pyc

# setuptools writes these; the leading-dot spellings never matched anything,
# so CI linted the copy of the sources left under build/ by `pip install .`.
build/
dist/

# Coverage artifacts. coverage.xml was committed once by accident (466a75b
# removed it) and both are regenerated by every local and CI test run.
.coverage
coverage.xml
junit.xml

# Sphinx build
_docs/
docs/_build/
docs/_generated_*.md
docs/api/autosummary/

# In this library
codex_session.sh
smonitor/_version.py
