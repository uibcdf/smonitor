# Reviewed for the 0.17.2 candidate before creating its tag.
version = "0.17.2"
route = "direct"
reason = "The 0.17.1 tag has stale citation metadata. This patch corrects the citation version and adds a pre-tag guard; Python runtime code, dependencies, Conda recipe, and installed command are unchanged. The 0.17.2 coordinate is unoccupied and the full matrix must pass at the exact candidate commit before publication."
decision_by = "Repository maintainer via Codex on 2026-09-26"
required_workflows = [".github/workflows/CI_full_matrix.yaml"]
