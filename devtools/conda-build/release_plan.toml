# Reviewed for the 0.17.3 candidate before creating its tag.
version = "0.17.3"
route = "direct"
reason = "This patch corrects the README release-status link and points to the independently verifiable Zenodo record. Python runtime code, dependencies, Conda recipe, and installed command are unchanged. The 0.17.3 coordinate is unoccupied and the full matrix must pass at the exact candidate commit before publication."
decision_by = "Repository maintainer via Codex on 2026-09-26"
required_workflows = [".github/workflows/CI_full_matrix.yaml"]
