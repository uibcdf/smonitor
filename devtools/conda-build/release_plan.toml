# Reviewed for the 0.17.0 candidate before creating its tag.
version = "0.17.0"
route = "staged"
reason = "The Conda console entry point and Windows installed-package gate changed; verify the exact staged package before public promotion."
decision_by = "Repository maintainer via Codex on 2026-09-26"
required_workflows = [".github/workflows/CI_full_matrix.yaml"]
